/**
 * This is an interface with a single method called parse(). It must be given a body with every class
 * that implements it.
 * 
 */
public interface XMLParser {
    
    public String parse();

}
